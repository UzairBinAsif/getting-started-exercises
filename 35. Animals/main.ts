let animals = ["Dog", "Tiger", "lion"]
for(let animal=0;animal<=(animals.length-1);animal++){
    if(animals[animal] === "Dog"){
    console.log(`A ${animals[animal]} could be a great pet.`);
    } else {
        console.log(`A ${animals[animal]} is a preditor & could be dangerous.`)
    }
}
console.log("These animals have a lot in commom like tooth, size, jump, etc.");